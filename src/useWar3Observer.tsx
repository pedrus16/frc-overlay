import { useEffect, useState } from "react"

const useWar3Observer = () => {
    const data = useState({})
    
    useEffect(() => {

    })

    return { data }
}

export default useWar3Observer